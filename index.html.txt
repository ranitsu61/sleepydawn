<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>폭신한 새벽</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    body {
      margin: 0;
      font-family: system-ui, sans-serif;
      background: linear-gradient(to bottom, #0f172a, #1e3a8a, #f6f1ff);
      min-height: 100vh;
      color: #111;
      overflow-x: hidden;
    }
    header {
      text-align: center;
      padding: 30px 20px;
      color: white;
    }
    .container {
      max-width: 700px;
      margin: 0 auto 100px;
      padding: 0 16px;
    }
    .card {
      background: rgba(255,255,255,0.9);
      border-radius: 16px;
      padding: 16px;
      margin-bottom: 16px;
    }
    input, textarea, button {
      width: 100%;
      margin-top: 8px;
      padding: 10px;
      border-radius: 10px;
      border: 1px solid #ddd;
      font-size: 14px;
    }
    button {
      background: #7dd3fc;
      border: none;
      cursor: pointer;
      margin-top: 12px;
    }
    .post {
      margin-top: 12px;
    }
    .like {
      cursor: pointer;
      margin-top: 8px;
      display: inline-block;
    }
    .comment {
      margin-top: 6px;
      font-size: 13px;
      background: #f1f5f9;
      padding: 6px 10px;
      border-radius: 8px;
    }
    .sheep {
      position: fixed;
      font-size: 32px;
      animation: float 6s ease-in-out infinite;
    }
    .sheep.one { top: 20px; left: 20px; }
    .sheep.two { top: 80px; right: 30px; animation-delay: 2s; }

    @keyframes float {
      0% { transform: translateY(0); }
      50% { transform: translateY(-12px); }
      100% { transform: translateY(0); }
    }
  </style>
</head>
<body>

<div class="sheep one">🐑</div>
<div class="sheep two">🐑</div>

<header>
  <h1>🌙 폭신한 새벽</h1>
  <p>청소년 수면을 위한 이야기 공간</p>
</header>

<div class="container">
  <div class="card">
    <h3>글 쓰기</h3>
    <input id="title" placeholder="제목" />
    <textarea id="content" placeholder="수면 팁이나 고민을 적어보세요"></textarea>
    <button onclick="addPost()">올리기</button>
  </div>

  <div id="posts"></div>
</div>

<script>
  const postsEl = document.getElementById('posts');
  let posts = JSON.parse(localStorage.getItem('posts') || '[]');

  function save() {
    localStorage.setItem('posts', JSON.stringify(posts));
  }

  function render() {
    postsEl.innerHTML = '';
    posts.forEach((p, i) => {
      const div = document.createElement('div');
      div.className = 'card post';
      div.innerHTML = `
        <h3>${p.title}</h3>
        <p>${p.content}</p>
        <div class="like" onclick="likePost(${i})">💖 ${p.likes}</div>
        <input placeholder="댓글" onkeydown="if(event.key==='Enter') addComment(${i}, this.value, this)" />
        ${p.comments.map(c => `<div class="comment">🐑 ${c}</div>`).join('')}
      `;
      postsEl.appendChild(div);
    });
  }

  function addPost() {
    const title = document.getElementById('title').value.trim();
    const content = document.getElementById('content').value.trim();
    if (!title || !content) return;
    posts.unshift({ title, content, likes: 0, comments: [] });
    save();
    render();
    document.getElementById('title').value = '';
    document.getElementById('content').value = '';
  }

  function likePost(i) {
    posts[i].likes++;
    save();
    render();
  }

  function addComment(i, text, input) {
    if (!text.trim()) return;
    posts[i].comments.push(text);
    input.value = '';
    save();
    render();
  }

  render();
</script>

</body>
</html>